# EMBArk Unit/Integration tests

To run the tests, execute `pipenv run python manage.py test` inside the `embark` folder.

*Note*: The DB has to be started. To do this, execute `docker compose up -d`.
