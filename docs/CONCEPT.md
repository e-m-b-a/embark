The deploy version works by putting everything that is used by the server into the /var/www directory.
EMBA and venv...everything

Meaning running the run-server.sh script deploys the server in that directotry and syncing some stuff between the 2.




Orchistrator:

Worker and host should have same OS else we can't use updater properly